#include "main.h"
/**
 * _isupper - Check if a letter is upper
 * @c : character received
 * Return: 1 is uppercase 0 otherwhise
 */
int _isupper(int c)
{
	if ((c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	return (0);
}


	
